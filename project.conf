# Project-wide configuration for backup scripts

# Path to the main backup script to install on remote hosts

# Example defaults for backup setup script
# You can override these in your own config


# Directory for installed backup scripts
BIN_DIR="$HOME/.local/bin"

# Directory for systemd user units
SYSTEMD_DIR="$HOME/.config/systemd/user"

# Log file location
LOG_FILE="$HOME/backup-sync.log"

# Path to the backup script to install
SCRIPT_PATH="src/shell/backup-main.sh"
